[build]
buildCommand = "pnpm run build:agent"

[deploy]
startCommand = "pnpm run start:agent"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
healthcheckPath = "/health"
healthcheckTimeout = 10
