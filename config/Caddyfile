# Replace huginn.yourdomain.com with your actual domain
{$DOMAIN:huginn.yourdomain.com} {
	# OpenClaw Gateway (WebSocket + HTTP)
	reverse_proxy openclaw:18789

	# Security headers
	header {
		X-Content-Type-Options "nosniff"
		X-Frame-Options "DENY"
		Referrer-Policy "strict-origin-when-cross-origin"
	}
}

# Syncthing web GUI (uncomment and set domain to expose externally)
# {$SYNCTHING_DOMAIN:sync.yourdomain.com} {
# 	reverse_proxy openclaw:8384
# }
